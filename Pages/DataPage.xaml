<Page x:Class="ShoesShop.Pages.DataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShoesShop.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DataPage">

    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <DataGrid/>
        
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <StackPanel HorizontalAlignment="Center" Margin="0,15,0,0">
                <TextBlock Text="Поиск" FontSize="20" FontWeight="Medium"/>
                <StackPanel Orientation="Horizontal" >
                    <TextBox Width="300" FontSize="28"/>
                    <Button x:Name="SearchButton"
                            Click="Search"
                            Content="⌕" 
                            FontSize="28" 
                            Width="40"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <Button x:Name="AddButton"
                        Click="Add"
                        Content="Добавить" 
                        FontSize="26" 
                        Height="40"
                        Width="340" 
                        Margin="0,0,0,15"/>
                
                <Button x:Name="DeleteButton"
                        Click="Delete"
                        Content="Удалить" 
                        FontSize="26" 
                        Height="40"
                        Width="340" 
                        Margin="0,0,0,30"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
