<Page x:Class="ShoesShop.Pages.DataPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ShoesShop.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DataPage">

    <Grid Background="{DynamicResource BackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="400"/>
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="DataGrid"/>
        
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <StackPanel HorizontalAlignment="Center" Margin="0,15,0,0">
                <TextBlock Name="SrcLabel"  Foreground="{DynamicResource ForegroundBrush}"  Text="Поиск" FontSize="20" FontWeight="Medium"/>
                <StackPanel Orientation="Horizontal" >
                    <TextBox Name="SrcBox"  Width="300" FontSize="28"/>
                    <Button x:Name="SrcBtn"
                            Click="Search"
                            Content="⌕" 
                            FontSize="28" 
                            Width="40"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Row="2">
                <Button x:Name="AddBtn"
                        Style="{DynamicResource button}"
                        Foreground="White"                        
                        Click="Add"
                        Content="Добавить" 
                        FontSize="24" 
                        FontWeight="Bold"
                        Height="50"
                        Width="240" 
                        Margin="0,0,0,15"/>
                
                <Button x:Name="DeleteBtn"
                        Style="{DynamicResource button}"
                        Foreground="White"
                        Click="Delete"
                        Content="Удалить" 
                        FontSize="24"
                        FontWeight="Bold"
                        Height="50"
                        Width="240" 
                        Margin="0,0,0,30"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
